import React from 'react';
import './AboutBat.css'; // Import the CSS file for styling

const AboutBat = () => (
  <div className="aboutbat-background">
    {/* Background image will be applied through CSS */}
  </div>
);

export default AboutBat;
