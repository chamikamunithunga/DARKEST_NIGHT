import React from 'react';
import './Home.css'; // Import the CSS file

const Home = () => (
  <div className="home-background">
    {/* No need to add elements here; the background will be applied via CSS */}
  </div>
);

export default Home;

